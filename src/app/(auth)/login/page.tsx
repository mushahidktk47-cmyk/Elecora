import { ComingSoon } from "@/components/shared/coming-soon";

export default function LoginPage() {
  return (
    <ComingSoon
      title="Log in"
      description="Accounts and authentication are being built in a later phase."
    />
  );
}
