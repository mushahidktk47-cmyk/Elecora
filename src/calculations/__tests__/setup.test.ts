import { describe, it, expect } from "vitest";

/**
 * Placeholder test — proves the test runner is wired up correctly.
 * Real calculation tests (e.g. Ohm's Law) get added in Phase 4
 * when the calculator engine is built.
 */
describe("test runner setup", () => {
  it("runs a basic assertion", () => {
    expect(1 + 1).toBe(2);
  });
});
